plugin: amazon.aws.aws_ec2

regions:
  - us-east-1

filters:
  tag:Name: wordpress-migration-ec2
  instance-state-name: running

hostnames:
  - instance-id

compose:
  ansible_connection: "'amazon.aws.aws_ssm'"
  ansible_aws_ssm_region: "'us-east-1'"
  ansible_aws_ssm_bucket_name: "'thompson-ansible-transfer-202937691499'"

groups:
  wordpress: true